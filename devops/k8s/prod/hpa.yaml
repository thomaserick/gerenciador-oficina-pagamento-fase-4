apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: gerenciador-oficina-pagamento-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: gerenciador-oficina-pagamento-api
  minReplicas: 1
  maxReplicas: 2
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
